import * as React from 'react'

export interface UploadFileProps {
    
}
 
const UploadFile: React.SFC<UploadFileProps> = () => {
    return (
        <div className="upload_file">upload fil</div>
    )
}
 
export default UploadFile